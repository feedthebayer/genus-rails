<section class="grp-newForm">
  <%= form_for [org, group], remote: true do |f| %>
    <%= f.text_field :name, class: 'grp-newTextfield',
      placeholder: "Enter a new group name" %>
    <%= f.submit class: 'grp-newSubmit' %>
  <% end %>
</section>
